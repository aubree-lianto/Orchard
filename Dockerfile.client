# ============================================
# ORCHARD ORCHESTRATOR CONTAINER
# ============================================
# Purpose: Runs Orchard orchestrator that uses ModelClient to communicate with LLM
# Used by: docker-compose.yml (orchestrator service)
# Contains: ModelClient, LangGraph agent logic, test scripts

FROM python:3.11-slim

WORKDIR /app

COPY inference/requirements.txt ./inference/requirements.txt

RUN python -m pip install --no-cache-dir -r inference/requirements.txt

COPY . .

ENV PYTHONPATH=/app

EXPOSE 8080

HEALTHCHECK --interval=10s --timeout=5s --start-period=10s --retries=3 \
  CMD python -c "import sys; sys.exit(0)" || exit 1

CMD ["python", "-u", "scripts/run_mock_client.py"]